plein d'infos, mais je dois juste écrire src/main.js. Let me build a self-contained bootstrap that wires everything. Since I only output one file, I'll make main.js self-contained (not relying on all the other modules, since they may not exist reliably). I'll implement the whole sim + render + HUD + input in main.js, using the global PIXI from vendor.

```javascript
```

Let me write a complete, runnable main.js.